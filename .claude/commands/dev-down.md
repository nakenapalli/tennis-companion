---
description: Stop the backend + frontend dev servers (./dev.sh down)
allowed-tools: Bash(./dev.sh:*)
---

Run `./dev.sh down` from the repo root to stop the backend and frontend (Postgres/Redis are intentionally left running). Reply in one short line confirming both are stopped.
